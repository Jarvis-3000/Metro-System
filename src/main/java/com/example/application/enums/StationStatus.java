package com.example.application.enums;

public enum StationStatus {
  OPEN, // The station is open and operational for passenger service
  CLOSED, // The station is closed temporarily, typically due to maintenance or safety
          // concerns
  UNDER_CONSTRUCTION, // The station is currently under construction and not accessible to passengers
}
